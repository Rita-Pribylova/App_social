let initialState = {}

type IninisialStateType = typeof initialState

const sidebarReducer = (state = initialState, action: any): IninisialStateType => {
    return state;
}

export default sidebarReducer;